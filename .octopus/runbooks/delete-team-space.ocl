name = "Tear Down (Delete) Team Space"
default_guided_failure_mode = "EnvironmentDefault"
description = <<-EOT
        **Action**: Delete a team tenant and space.
        
        **Affects**: Target team space ONLY.
        EOT

connectivity_policy {
    allow_deployments_to_no_targets = true
}

run_retention_policy {
    quantity_to_keep = 100
}

process {}